<header class="section-header">
  <h1 class="section-header__title">{{ page.title }}</h1>
</header>

{% comment %}
  Regular page content goes here.
{% endcomment %}
<div class="rte">
  {{ page.content }}
    {% if page.title == 'My Rewards' %} <!-- use same title as the rewards page you created -->
	    {% include 'sam_account' %}
    {% endif %}

</div>
